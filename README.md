The main executable requires the user to input the filepath of the competition set.

The user may also need to install the imblearn package with \
`pip install imblearn` \
in order to run the main executable.

